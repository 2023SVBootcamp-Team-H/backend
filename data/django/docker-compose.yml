version: "3"

services:
  web:
    build: . # build by Dockerfile in current dir.
    working_dir: /code 
    command: python manage.py runserver 0.0.0.0:5000
    ports:
      - "5000:5000"
    volumes:
      - ./dontworry:/code
    restart: always

